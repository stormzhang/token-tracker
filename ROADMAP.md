# ROADMAP — token-tracker

本地 AI Agent Token 消耗追踪：Claude Code / Codex 的状态栏显示 + CLI Dashboard。
本文件是项目真实进度源，记录当前阶段、已完成、待办、阻塞与最近验证。`README.md` 写介绍与用法，`CLAUDE.md` 写开发约定，三者分工不混。

## 当前阶段

`0.4.5` 已发布 PyPI（2026-06-25，https://pypi.org/project/token-tracker/0.4.5/ ），commit `b56d9b9` + 打了 `v0.4.5` tag。**Codex statusline python 路径死链自动修复**：`_install_codex_statusline` 看到 codex-statusline 段已存在就无脑幂等返回，从不检查 command 里的 python 路径是否还活着；用户升级 Python 大版本、切 conda/pyenv/asdf 环境、或卸载某 Python 后，命令指着死 python，statusline 脚本 import `token_tracker` 失败被 try/except 吞掉，状态栏只剩项目名 + git diff（Total/Model/L2 全没）。修复：检测已有段的 python 是否跟当前要装的一致，不一致就删旧段装新段；用户升级 Python 后跑 `tt setup` 自动恢复。补 `test_codex_statusline_install_updates_stale_python` 回归。期间另把 install.sh 增强 PATH 健康检查：当前 shell 解析到的 tt 跟刚装的不一致时给警告 + 卸载老 conda/system pip 装的旧版指引（commit `5c39c1c` 已 push，不绑 PyPI 包）。

`0.4.4` 已发布 PyPI（2026-06-25，https://pypi.org/project/token-tracker/0.4.4/ ），commit `e911868` + 打了 `v0.4.4` tag。**Windows 紧急修复**：用户反馈 Codex `config.toml` 解析崩溃——tt 写 `[[hooks.Stop]]` 段时用 TOML basic string `"..."` 包裹 command，Windows 路径里 `\U`/`\A`/`\P` 等被当 unicode 转义起始符触发 `too few unicode value digits`。改用 TOML literal string `'...'`（单引号无转义）；regex 同时认双引号 + 单引号兼容老用户；补 `test_codex_statusline_windows_path_toml_parses` 用真 Windows 路径 + tomllib 解析回值的回归测试。

`0.4.3` 已发布 PyPI（2026-06-25，https://pypi.org/project/token-tracker/0.4.3/ ），commit `7c4bb7b` + 打了 `v0.4.3` tag。含五批改动：① **会话表精简**——`tt status` 底部 + `tt sessions` 共用的会话表删「活跃/时长」列、模型只显主力一个（连带删 `_fmt_session_duration` 函数 + `unit_hour` i18n 孤儿）；② **README 全套重构**——daily 截图置顶、StatusLine 章节提到报表速览前作为明星功能（突出 Codex 业界首创伪 statusline）、CC 四行字段表折叠 `<details>`、精简实现细节（去 Stop hook / systemMessage 等用户不必关心的字）、卸载说明精简到 `tt unsetup`、删开发 + TODO 段；8 张截图全部 256 色无损压缩（仓库截图体积约 -50%）+ 新增 sessions / themes 两张；③ **install.sh 修复 `curl|bash` 安装崩溃**——避开 prompt_toolkit upstream issue #1943（curl 管道 stdin + `< /dev/tty` 重定向触发 macOS kqueue `OSError [Errno 22]`），改为不重定向 stdin、走 `_auto_setup` 默认全装，配合 `auto_setup_hint` 换行 + 文案精简引导用户独立终端跑 `tt setup` 自定义；④ **CI 测试时区敏感修复**——`test_build_status_data_merges_and_per_agent` 用 `hours=2` 跨 UTC 凌晨被 today 过滤致 CI 间歇失败，改成 30~40 min（本地复现 + 验证）；⑤ **gitignore 补 `.mypy_cache/`/`.ruff_cache/`/coverage 产物/`*.log`**。

`0.4.2` 已发布 PyPI（2026-06-24，https://pypi.org/project/token-tracker/0.4.2/ ），commit `2ec41d5` + 打了 `v0.4.2` tag。含三批改动：① `install.sh` tty 判定 fix（`400fb70`）；② 升级引导改「自动走一遍 setup」+ `SETUP_VERSION` bump 到 2（`da670e7`）；③ 国产模型识别 + cost（七家：Kimi/GLM/Qwen/Doubao/DeepSeek/MiniMax/MiMo）+ 国外补强（Grok 定价、Gemini 短名，`bceb903`）。

`0.4.1` 已发布 PyPI（2026-06-23，https://pypi.org/project/token-tracker/0.4.1/ ），打了 `v0.4.1` tag。**注**：`0.4.0` 之前打过 tag 但实际上**已上 PyPI**（之前 ROADMAP 写错）。

## 已完成（均已发布并验证）

**核心能力**
- 状态栏：Claude Code 三行布局（会话时长 / Cache 命中率 / Token 增量 / 重置倒计时 / Git 分支）+ Codex 官方 `status_line`；主题系统（mocha / dracula / default）；宽度自适应 + 终端尺寸实时检测
- CLI Dashboard：交互式（备用屏 + 按键 + vim 键位 + ESC 退出），启动自动检测 setup 状态并支持脚本版本更新
- 报表：`daily` / `weekly` / `monthly` / `sessions`，多 Agent 来源分组、排序功能
- 数据源：Claude / Codex 适配器，`registry` 探测可用 agent，`rate_limits` 采集
- 成本定价：litellm 在线表（7 天缓存）+ `_fallback_pricing` 离线底座，模型识别，5 小时块 + p90
- i18n：zh / en 跟随系统语言，支持 `TT_LANG` 覆盖

**工程**
- `ruff` / `pytest` / `mypy` 配置；CI；PyPI 发布（`pip install` + curl 一键安装脚本）
- 测试基线：6 个测试文件、55 个用例

**近期版本迭代**
- `0.3.7`：修复 Opus 4.8 成本估算为 0
- `0.3.8`：识别 Claude Fable 5 定价 + 未知模型显形（不再静默归零）
- `0.4.0`：包目录迁移标准 src layout（导入名 `src` → `token_tracker`）+ CI 适配新包名 + README 双语同步（**实际未发布**，并入 0.4.1 一起出）
- `0.4.1`：大批量功能与重构合并发布——`tt` 默认走 `daily`、daily 顶部三段单 Panel 概览（Last 12 months / This Month / This Week）；老用户升级自动重新引导（SETUP_VERSION 机制）；六套主题统一系统 + `tt theme` 命令 + 首次运行交互向导；Codex 伪 statusline 接入主题、两行布局；CC statusline 四行布局 + git diff + TPS + Code 行数；`tt status` 替代旧 dashboard、过去 5h 三段实时面板；`tt sessions` 取最近 N 条再按 cost 排（修「史上高 cost 霸榜」）；柱状图全窗口 + 暗色基线；activty_minutes 维度；config 文件统一 `config.json`；Codex 不再接管 `status_line`；产物全集中放 `~/.config/token-tracker`；install.sh 多安装方式阶梯（uv / pipx / venv / pip）绕开 PEP 668；i18n 完善（月份表头、d/h 单位、Active Days）；codex-auto-review 改写为 gpt-5.5 + 补 gpt-5.5 定价。完整改动见 `git log v0.4.0..HEAD`（约 100 commit）

## 已随 `0.4.1` / `0.4.2` 发布（已实现并验证）

- **`tt daily` 重构为 GitHub 风格 token 贡献热力图**（2026-06-16）：全角 ■ 深浅绿方格（5 档）+ 月份表头 + 紧凑总览 + 图例，替换原逐日表格。
  - 新增 `ui/heatmap.py`（渲染 + 自带紧凑总览）；`ui/theme.py` 加 5 档 GitHub 绿 + 分位分档；`ui/console.py` 加 `forced_color_console()` 强制 24-bit truecolor。
  - 自适应：按终端宽度决定显示周数（最多一年），`soft_wrap` 防折行；总览自渲染（不复用 dashboard 宽 header），半屏不折。
  - 会话内彩色：在 Claude Code 里用户敲 `!tt daily` 才渲染真彩色（slash command 注入、模型工具输出都会 strip 颜色，唯用户 `!` bash 模式渲染 ANSI —— 已实测确认）。
  - **原「会话内 skill 替代 + 删除终端 daily」设想撤销**：终端命令是彩色热力图的唯一载体，删了功能即失。
  - 新增 `tests/test_heatmap.py` 6 用例。
  - **daily 跟随当前会话 agent**（环境变量识别）：CC 会话只显示 CC、Codex 会话只显示 Codex；独立终端暂保持合并（待后续处理）。
  - 修复 `■` 实为 1 列宽导致的月份/方块错位（`_CELL_W=2`）；拿不到终端宽度（Claude Code `!` 非 tty）时默认显示整年。
  - 总览改紧凑卡片（`Panel` expand=False，贴合内容不撑满）+ 数据前置标签；daily 不再打 Detected 行。
- **全 CLI 配色切到 Catppuccin Mocha / Latte 双 flavor**（2026-06-18）：`ui/theme.py` 重构——`_MOCHA`/`_LATTE` 官方调色板（truecolor）映射到 `_S` 语义槽位，暗终端 Mocha、亮终端 Latte 按 `COLORFGBG` 自动选，`TT_THEME=mocha/latte` 手动覆盖（兼容旧 light/dark）；热力梯度出 `_HEAT_MOCHA`/`_HEAT_LATTE` 双版。dashboard / daily / weekly / monthly / sessions 全部统一（`panels.py`/`tables.py` 硬编码 blue → `_S.blue`），和 statusline 同源。
- **daily 概览卡片**（2026-06-18）：紧凑卡片 = 品牌行（Token Tracker 红 + agent 暗红）+ 红分割线 + `Last 12 months` 周期标题（带日期范围）+ 过去一年三行分析：橙 Tokens/Cost/Sessions/Avg-Cost/Active-Days、蓝 Peak/Current·Longest Streak、粉 Busiest/Top Model/Active Hour，与 weekly 顶部样式统一。数据筛过去一年；Active Hour = 过去一月按**北京时区（UTC+8，固定不随系统）**聚合 token、取活跃小时区间（支持跨午夜）；Current/Longest Streak = 当前连续（末尾段延续到今/昨才有效，否则归 0）·历史最长连续活跃天数（去重日期算）。配色走 Catppuccin 语义槽位、具体色值见 `theme.py`。
  - 几轮迭代后**删除** Top Project（连带 `proj_tokens`）与 Longest Session（连带 `sessions` 参数链、`_fmt_duration`/`SessionStats` import）；`Max Streak` 改为 `Current/Longest Streak`。
  - Active Days 只统计**本地日志覆盖到的活跃天**（CC `~/.claude/projects`、Codex session 文件保留有限，更早的天无数据可读），非 bug。
- **`!tt` 非 tty 宽度探测修复**（2026-06-18，已提交 73916af）：`ui/console.py` 加 `_forced_width()`，从 `_P9K_TTY`/`SSH_TTY` ioctl 取真实终端宽度（忽略 Claude Code 子进程置的占位 `COLUMNS=0`），daily 周数判定交回 Rich console。
- **`tt weekly` 重构为五区块周报**（2026-06-18）：① This Week 卡片（品牌行 Token Tracker + agent、红分割线；第二行 Tokens/Cost/Avg-Cost 橙、第三行 Sessions/Msgs/Active Days 蓝，均带环比上周；Avg/Cost=本周成本÷已过天数）② Daily Trend 每日 token 垂直柱状图（近 30 天，前三天亮、峰值标日期、底部起止）③ Weekly Trend 逐周进度条 ④ Project Trend ⑤ Model Trend（均本周口径）。四块各用主色（橙/绿/粉/蓝），标题/名称列/进度条/分割线统一该主色，进度条用同色深浅，突出项（Weekly Trend 本周 / Project·Model Trend 第一名）用亮主色、其余压暗；Weekly/Project/Model Trend 各限 8 行。
  - 数据层：`WeeklyStats`（连带 Daily/Monthly）加 `projects` 字段 + `_aggregate` 按 project 累加（同 models 机制）；新增 `test_aggregator.py::test_aggregate_fills_projects_by_token`。
  - weekly 也跟随当前会话（原仅 daily）：CC 会话只看 CC、独立终端合并；去掉 weekly 的 Detected 行。
  - daily / weekly 品牌行统一为 `Token Tracker: a + b`（冒号 + 连接、去圆点、跟随会话）。
  - 渲染走 `forced_color_console`，`!tt weekly` 非 tty 也彩色；配色沿用 Catppuccin 语义槽位。
- **statusline 标签微调**（2026-06-18）：`Context` 标签缩写为 `Ctx`（缩短状态栏宽度，`HOOK_VERSION` → 1.8，老用户升级 pip 后自动同步 `~/.claude/tt-statusline.py`）；进度条维持原样（█ 三段色填充 + ░ 空槽，期间试过 ▄/满格双色/灰底/蓝紫灰/浅蓝紫均回退）。
- **daily / weekly 署名 footer + 缩进对齐 + `tt -v`**（2026-06-18）：四个 Trend 区块（Daily/Weekly/Project/Model Trend）+ daily 热力图整面板统一左缩进 2 格（对齐卡片内容；daily grid 宽度判定预留 2，`_display_weeks` 用 `width-6`，防折行）；weekly 底部、daily 图例 `More` 右侧（空 4 格）加 dim 署名 `tt · by stormzhang`（左对齐缩进 2）；`tt -v` 输出全名 `token-tracker <版本>` + `by stormzhang · GitHub 链接`。
- **UI 去重重构**（2026-06-18）：提取 `brand_line` / `append_metric` 到 `format.py`，daily（heatmap.py）与 weekly（tables.py）品牌行 + 指标渲染共用、消除重复；渲染输出逐字不变。
- **daily/weekly 稀疏 + 窄终端自适应**（2026-06-18）：① Weekly Trend 取消本周高亮、逐周统一亮绿（本周恒在最上无需另标）；② daily 概览三行指标按终端可用宽贪心折行（`emit_metrics`，窄终端不溢出卡片）；③ Daily Trend 稀疏自适应——去前导空白天（保底 7 天）+ 中间 > 5 天连续空压成 3 列「·」gap + 标题显实际跨度；④ daily 图例署名窄终端自动另起一行、对齐 Less 列、空行隔开（避免终端硬折导致折行掉色），宽终端仍接图例右侧。
- **会话内彩色报表 hook 产品化（终端 CLI）**（2026-06-18）：`tt setup` 自动注册会话内彩色命令——CC `/tt-daily`·`/tt-weekly`（`UserPromptExpansion`）、Codex `ttdaily`·`ttweekly`（`UserPromptSubmit`），`block`+`reason` 直接渲染真彩色报表、不经模型、不污染上下文。`hooks.py` 加 `CC_REPORT_HOOK_SCRIPT`/`CODEX_REPORT_HOOK_SCRIPT` 内嵌模板（`sys.executable -m token_tracker.cli` 调用、独立 `REPORT_HOOK_VERSION`、宽度探测 -14、Windows 跳 ioctl）+ 扩展 `_setup_*`/`_unsetup_*`（特征码 `tt-report-hook` 幂等合并、不覆盖用户 hook、CC 写 commands/*.md + 合并 UserPromptExpansion 数组、Codex 末尾追加 `[[hooks.UserPromptSubmit]]` 零依赖）+ 4 个测试 + README 双语（含「仅终端」限制）。
- **统一多主题系统**（已完成 7 阶段，方案见 `~/.claude/plans/modular-chasing-pine.md`）：把现在割裂的两套配色（CLI `ui/theme.py` Catppuccin Mocha/Latte vs statusline `hooks.py` 内嵌 256-color + 写死 mocha）统一成一套主题源，主题集扩到 Catppuccin 全家（Mocha/Latte/Frappe/Macchiato）+ Nord + Dracula + default，支持预览、`tt theme` 切换、首次运行交互向导。
  - **阶段 1 ✅ 地基（不接线）**（2026-06-18）：新增 `ui/themes.py`（7 主题 × 9 基色 + 5 档热力 + `is_light`；mocha/latte 迁现值、frappe/macchiato 官方 hex、Nord/Dracula 按色相就近映射 9 槽、default 用 3-bit 兼容色名；`derive_slots` 9→17 语义槽、`_STATUSLINE_SLOTS` 10 key 子集、`theme_to_statusline_ansi` hex→truecolor/default→3-bit）+ `config.py`（`~/.config/token-tracker/theme.json` 读写 + `resolve_theme` 优先级链 `TT_THEME`>配置>`COLORFGBG`>mocha、兼容旧 light/dark）+ `tests/test_theme.py` 12 用例。官方 hex 逐字段核对（catppuccin/palette、nordtheme、dracula README）。
  - **阶段 2 ✅ `_S` 运行时化（接线 CLI）**（2026-06-18）：`ui/theme.py` 删旧 `_MOCHA`/`_LATTE`/`_is_light_theme`/`_HEAT_*`，`_S` 改成 `_SProxy` 运行时代理（`__getattr__` 取当前激活主题的 17 槽，60+ 处 `_S.token` 调用零改、KeyError→AttributeError）；加 `get_active_theme(_name)`/`set_active_theme`/`preview_theme`（上下文管理器）/`heat_greens()`（取代模块级 `HEAT_GREENS`）；`heatmap.py` 引用随之改。CLI 报表配色现在走 `config.resolve_theme()`，`TT_THEME=dracula/nord/...` 即时生效。新增 5 用例（代理跟随/未知属性/preview 还原/heat 跟随）。
  - **阶段 3 ✅ statusline 同源烘焙**（2026-06-18）：`hooks.py` 把 statusline 脚本里写死的 `THEME="mocha"`/256-color `THEMES` 换成占位符 `__STATUSLINE_THEME_COLORS__`（当前主题 truecolor）+ `__STATUSLINE_DEFAULT_COLORS__`（3-bit 兜底）；`_render_hook_script` 注入 `themes.theme_to_statusline_ansi(config.resolve_theme())`；脚本内 `C = THEME_COLORS if _supports_color() else DEFAULT_COLORS`。`HOOK_VERSION` 1.8→1.9（老用户 pip 升级后自动重烘焙）。statusline 改 truecolor 烘焙（后按主人审美把 mocha 状态栏配色调回旧观感，`_STATUSLINE_SLOTS` 与 CLI `_S` 不完全同源，见最近验证 2026-06-19）。
  - **阶段 4-5 ✅ `tt theme` 命令 + 预览**（2026-06-18）：`cli.py` 加 `theme` 子命令（自管、不触发 auto-update/agent 检测）——`show`（当前主题 + 来源 env/config/auto）、`list`（7 主题 × 8 色块 + ● 当前标记）、`set <name>`（`config.save_theme` + `set_active_theme` + 已配则 `update_hook` 重烘焙 statusline + env 覆盖警告）、`preview <name>`（`preview_theme` 渲染 CLI 语义色/三段进度条/热力阶 + statusline 示例行）、`tt theme <name>` 简写 = set。i18n 加 10 条文案、`available_cmds` 加 theme。+5 测试。
  - **阶段 6 ✅ 交互向导**（2026-06-18）：新增 `wizard.py`（`run_wizard`：欢迎 → 列主题 → Prompt 选 → 渲染所选主题预览 → `save_theme` + `setup()` 落地）；`cli.py` 首次运行钩子改为 `_should_run_wizard()`（双 tty 且非会话内才进，否则降级静默 `setup(auto=True)`）；延迟 import 破 cli↔wizard 循环。向导聚焦主题选择，agent 自动检测、彩色命令随 setup 默认装、语言跟随系统（对原 plan 四问的合理收敛，避免改 setup 签名链）。+2 测试（判定三态 + happy path）。
  - **阶段 7 ✅ 文档收尾**（2026-06-18）：README 双语加「配色主题」节（主题表 + `tt theme` 用法 + 向导 / `TT_THEME` 说明）+ 功能列表改「多主题统一配色」+ 使用表加 `tt theme`；CLAUDE.md 加「主题系统约定」节 + 项目结构表（themes/config/wizard）+ 命令行；ROADMAP 同步。
- **Codex 虚拟 model `codex-auto-review` 改写 + gpt-5.5 定价**（2026-06-22，已提交 `1ceb26a`）：`adapters/codex.py` 加 `_rewrite_virtual_model`，把 Codex stop-time auto-review gate 的虚拟 model name `codex-auto-review` 改写成背后真实模型 `gpt-5.5`，避免它在 Model Trend / sessions 等报表独占一行、同时让 cost 走真实定价。`cost.py` 补 `gpt-5.5` 内置价（input 5 / output 30，是 gpt-5 的 4 倍，必须专属、不能走系列兜底）+ `_FAMILY_FALLBACK` 加 `codex-` 前缀兜底做双保险。`tables.py` Model Trend 加 `min_pct=2` 长尾过滤、与 Project Trend 对齐。补 `_rewrite_virtual_model` / gpt-5.5 定价 4× / auto-review 兜底三组用例。
- **Codex 伪 statusline 接入主题系统**（2026-06-22）：原本配色硬编码 mocha、不跟随主题；现改为**烘焙注入**——`_render_codex_statusline_hook()` 注入 `themes.theme_to_statusline_ansi(resolve_theme())` 的 truecolor 字典，脚本内统一走 `C["..."]` 槽位（项目 `project` / 分支 `branch` / 增删 `added`·`deleted` / 进度条 `bar_ok·warn·danger` / Limit·Ctx 标签 `label` / **Total → `tokens`(橙) / Model → `total`(红)**）。Codex TUI 实测支持 truecolor，只注入一套（不像 CC statusline 还需 256 兜底）。`tt theme set` → `is_setup()` → `update_hook()` 现成链路自动重烘焙 Codex 脚本，无需新增触发。至此三套配色（CLI 报表 / CC statusline / Codex 伪 statusline）全部同源跟随主题。`STATUSLINE_HOOK_VERSION` 1.4→1.6。
- **报表柱状图全窗口显示 + 暗色基线**（2026-06-22，已提交 `b3f4e8a`）：daily Daily Trend 改固定最近 30 天全显示（去前导裁剪 + 中段空段「·」压缩），monthly Weekly Trend 改固定最近 30 个日历周（原 20、且只显示有数据周 → 现全窗口，缺失/没用过的周补 0）。无可见高度的天/周（空白、或量极小不足一格）最底行画一格更暗基线 ▁（`_darken(_S.peach,0.4)`+dim），与真实矮柱（dim peach）拉开、一眼可辨、时间轴连续。Model Trend 与 Project Trend 一致过滤占比 ≤2% 长尾项。`test_tables.py` 补 frozen `now`（固定 30 周窗口测试稳定）、mock `week` 改 monday ISO 日期对齐 `aggregate_weekly` 真实契约。
- **`tt sessions` 取数纠错 + codex 补真实 duration**（2026-06-22，已提交 `25fbfb7`）：原「全量按 cost 倒序取前 20」致史上高 cost 会话恒久霸榜、新会话和低成本 agent（codex）永远进不了榜 → 改为**先按时间取最近 N 条、再按 cost（或 `--sort`）展示**。过滤口径从活跃时长 `active_minutes` 改为会话跨度 `duration_minutes`（cc + codex 统一）。codex 每会话仅 1 条汇总 entry、原 duration 恒为 0 被全滤；`UsageEntry` 加 `session_end` 字段、`adapters/codex.py` 取末事件 timestamp 填入、`aggregate_sessions` 据此算真实跨度（claude 留 None 走多条 entry 原逻辑）。补 `session_end` 解析 + 单条 entry 真实跨度两组用例。
- **国产模型识别 + cost（七家）**（2026-06-24）：`cost.py` `_fallback_pricing` 补 30 个国产 model 内置价 + `_FAMILY_FALLBACK` 加 10 条系列前缀兜底，`format.py` `MODEL_SHORT` 配套短名，覆盖 Kimi/Moonshot、智谱 GLM、阿里 Qwen、火山 Doubao、DeepSeek、MiniMax、小米 MiMo。**定价口径**：国产按各家中国站人民币价 ÷7.1 折 USD（与 CC/Codex 同口径）；**GLM 例外**用 z.ai 国际站官方 USD（中国站含缓存完整价是 SPA、官方页抓不到）；阶梯定价模型（Qwen3-Coder / Doubao）统一取 0-32K 基础档。七家官方页 2026-06 核实并修正草案（`kimi-k2-instruct`/`turbo` 已 EOL → 改 K2.7/K2.6/K2.5；DeepSeek 进 V4、chat/reasoner 映射 v4-flash；小米 MiMo 与 DeepSeek 同价、Pro 主攻 agentic 编程）；已下线旧 id / 未来新版本走系列兜底不归零。新增 `_cny`/`_usd` 汇率 helper（汇率常量 `_CNY_PER_USD=7.1` 集中）。**另补国外**：xAI Grok 官方定价（`grok-4.3` / `grok-build-0.1` / `grok-code-fast-1`，litellm bare 零收录会归 $0；2026-05-15 退役 slug 靠系列兜底按官方路由对齐 grok-4.3 / build-0.1）+ Gemini 8 个短名（litellm 价已对、只补显示名不入兜底）。`test_cost.py` 共 +10 用例。
- **第三方配额对接 Provider 框架**（2026-06-25，已提交 `900e599`）：新增 `providers/` 包（`base.py` / `script.py` / `__init__.py`），`RateProvider` ABC + `ProviderRateData` + 注册表 + 缓存；`ScriptProvider` 执行外部脚本获取配额；`rate_limits.py` `load_rate_limits()` 改为链式查询（官方 → provider → 降级）；`types.py` `RateLimits` 扩充 `monthly_pct` / `monthly_resets_at`；`cli.py` 加 `tt quota` 诊断命令；`hooks.py` CC 状态栏无官方配额时自动走 provider（`HOOK_VERSION` 1.7→1.8）。新增 `tests/test_providers.py` 共 19 用例。`uv.lock` 无关变更未提交。

## 待办 / 计划

- **push `0.4.2`（待主人确认）**：本地已 commit（`2ec41d5`）+ tag `v0.4.2` + PyPI 已上传，待 `git push origin main` + push tag（`v0.4.1` / `v0.4.2` 两个历史 tag 一起）
- 桌面版（Tauri）规划：图表可视化、数据钻取、实时监控、多 Agent 多模型监控（仅规划，未启动）
- **会话内彩色报表 hook — 桌面多形态适配**（终端部分已落地，见「进行中」）：桌面 app / web GUI 不吃 ANSI（实测乱码），需按形态输出 HTML/markdown（`tt` 加 `--format`、hook 检测形态）。详见本地 `docs/cc-hook-tt-真彩色.md`。
- **远程下发通知通道**（📝 规划，新分支 `feature/broadcast` 做）：纯下行——读本地 `~/.claude/tt-broadcast.json`，在 **statusline 第 5 行**（未过期里优先级最高一条、按 level 着色）+ **`tt status` 头图上方横幅**（全部未过期）展示远程下发的公告/告警/进度；level 分流 `urgent`/`info`/`progress` + `expires_at` 自动过期；配 `tt broadcast` 写入命令自测。**只接收不上传、不碰隐私定位**。同步链路（远程→文件）+ 排行榜 PK（需上行、必 opt-in + 脱敏）留后续。详见 `docs/broadcast-plan.md`。
- `mypy src` 有 5 个历史遗留报错（`aggregator.py` / `cli.py`）：准则是**别新增**，不顺手改无关旧报错

## 阻塞

- 无技术阻塞。`0.4.1` 发布需主人逐步确认（红线：tag push / main push / PyPI 上传）。

## 最近验证

- **2026-06-24**：**国产模型识别 + cost 七家落地并验证**。`cost.py` 补 30 个国产内置价（Kimi/Moonshot 6 + GLM 5 + Qwen 3 + Doubao 4 + DeepSeek 4 + MiniMax 5 + 小米 MiMo 2）+ 10 条系列前缀兜底，`format.py` 配套 30 短名。定价用并行 subagent 逐家查官方页（2026-06）：国产按中国站人民币价 ÷7.1 折 USD、GLM 用 z.ai 国际站 USD（中国站 SPA 抓不到完整价）、阶梯模型取 0-32K 档；小米 MiMo 从 `mimo.mi.com` 官方页核到（与 DeepSeek 同价、Pro ¥3/¥6、标准 ¥1/¥2）；`moonshot` 系列兜底收窄为 `moonshot-v` 避免误吃 `moonshotai/` vendor 前缀。`test_cost.py` +8 用例（口径换算 / GLM 国际站 / MiMo / 系列兜底 / 短名不漏）。`pytest` 全绿、`ruff` 全过、`mypy` 仅 1 历史报错无新增。**真实环境核验**（`get_pricing()` 含 litellm 在线表）：所有国产 model（含旧 id / 未来版本 / 小米 UltraSpeed 变体）全部非 $0、系列兜底全生效、数字逐个核对。**已知偏差**：`deepseek-chat`/`reasoner` 当前被 litellm 在线表旧 V3.2 价（$0.28/$0.42）覆盖、暂高估约 2 倍（官方现映射 V4-Flash ¥1/¥2），litellm 更新后自愈、离线兜底已是 V4 价；`deepseek-v4-flash`/`pro` 直连名不受影响。**国外补强**（同日）：补 xAI Grok 官方定价（`grok-4.3` $1.25/$2.5、`grok-build-0.1`/`grok-code-fast-1` $1/$2，cache $0.2；litellm bare 零收录会归 $0；2026-05-15 退役的 `grok-4-fast`/`grok-4.1-fast`/`grok-3` 靠系列兜底按官方路由对齐 grok-4.3、`grok-code-*` 对齐 build-0.1）+ Gemini 8 短名（litellm 价已对、纯显示名）；`mimo.mi.com` 同理。真实环境核验 Grok 走 fallback、退役 slug 路由正确、Gemini 短名生效。`test_cost.py` 31 用例。
- **2026-06-23**：**升级引导改为「自动走一遍 setup」+ `SETUP_VERSION` bump 到 2**（已提交 `da670e7`，0.4.2 内容）。背景：SETUP_VERSION 机制初版（`e1f6ee9`）在「非 tty / 会话内」只打印一行提示、不真正配置——而主人主力在 Claude Code 会话内用 tt，等于永远只看到提示、新功能不被启用，达不到「老用户升级后都走一遍 setup」的预期。① **`cli.py`** 触发块从「真终端 `run_wizard()` / 否则打印 `setup_outdated_hint`」改为**统一调 `_run_setup_flow()`**（与「没配过」路径同一入口），由它按环境分流：真终端弹 wizard 让用户选、会话内 / 非 tty 静默 `_auto_setup` 用默认值全装（语言跟随系统 / mocha / 组件全开）+ `auto_setup_hint` 提示；两条都最终 `setup()` → `save_setup_version()`。② **`config.py`** `SETUP_VERSION` 1 → 2，覆盖所有现存用户（0.3.8/0.4.0=0、0.4.1=1，全 < 2）升级后重走一遍；注释强调「只能整数、一次 +1」（用 float 如 1.1 会被 `int()` 截断致无限触发）。③ **`i18n.py`** 删孤儿 `setup_outdated_hint`（中英）。④ **测试** 4 → 3 用例（触发→断言走 `_run_setup_flow`、已最新→不触发、setup 写版本）。⑤ **会话内弹不出交互 wizard 是架构硬约束**（questionary 上下键依赖真 tty + 不能污染 AI 回复流），会话内只能静默按默认配；要交互选得真终端跑。`pytest` **115 全绿**、`ruff` 过、`mypy` 1 历史无新增；隔离临时目录端到端实跑：会话内老用户（`setup_version=1`）→ 触发 → `_auto_setup` 静默配好（lang=zh / theme=mocha）→ 写回 `setup_version=2`，下次不再触发。另：主人真实 config 实测已是 `setup_version=2 + codex_faux_statusline=false`，证明真终端 `uv run tt` 跑过 wizard 且交互选过（auto 默认全开=true，false 只能来自交互选 No）——真终端弹 wizard 这条路径已被主人本人验证。
- **2026-06-22**：**`tt` 默认走 `tt daily` + 顶部三段概览合并到单一 Panel**（已提交 `acfb499`）。动机：把 daily 推到默认入口（热力图比 5h 实时面板更适合「开机看一眼」的概览场景），同时在顶部加自然月当月 + 当周的总数据，方便快速对比近期趋势。① **`cli.py`** 默认命令 `status` → `daily`（`:371`）；daily 分支多算两份 `_aggregate_per_agent(report_agents, aggregate_weekly/monthly)` 喂给 `render_daily_heatmap`，那一行加 `# type: ignore[operator]` 不新增 mypy 报错。② **`ui/heatmap.py`** `render_daily_heatmap` 加 `weekly` / `monthly` 参数；`_render_summary` 重写为**单一 Panel** 内拼三段——共享品牌行 + 红 Rule 主分割，段间用 dim Rule 区分，**Last 12 months** 保留原橙/蓝两行指标（**删掉粉色第三行**「最忙 / Top Model」），**This Month / This Week** 各只一行橙色 Tokens / Cost / Avg/Cost（带环比 ↑/↓ 上月/上周）+ 活跃天数 X/月总天数 或 X/7；整面板自带 `Padding(0,0,0,_INDENT=2)` 缩进 2。复用 `tables._merge_weeks` / `_merge_months` / `_month_span` 三个 helper（不复用 `_render_week/month_summary` 整套——它们包独立 Panel、不适合作子段）。③ **`i18n.py`** 清理删第三行造成的孤儿 key（`daily_busiest` / `weekday_full` 中英共 4 行）；`heatmap.py` 顶部 import 清掉 `defaultdict` / `_model_short`（也是孤儿）。④ **样式与主题**：所有配色走 `_S.peach/blue/good/red/dim` 主题代理，**自动跟随主题**；标签如 `active_days` / `daily_peak` / `daily_streak` 已 i18n、跟随语言，`Tokens` / `Cost` / `Avg/Cost` / `This Month` / `This Week` / `Last 12 months` 沿用 weekly/monthly 命令现状的英文产品标签保持一致（如需全面 i18n 是更大话题、不属于本次）。⑤ **`tt weekly` / `tt monthly` 命令行为零回归**——未碰 `tables.py`（中途加 `indent` 参数的方案被回滚掉了）。`pytest` **115 通过** + 1 个 test_status 基线失败（与本次改动无关，stash 验证）；`ruff` 全过；`mypy` 仍 1 个历史报错、无新增。实跑核对：`tt` 无参与 `tt daily` 一致输出单 Panel 三段、Last 12 months 卡片中显示橙/蓝指标行、This Month/This Week 各一行带环比+活跃天数、热力图正常。
- **2026-06-22**：**老用户升级后自动重新引导一次（SETUP_VERSION 机制）**（已提交 `e7d3116` + `e1f6ee9`）。动机：新版加了配置项（如语言/主题/Codex 伪 statusline），老用户之前没机会选，需要被引导一次；但又不能每次升级都打断。① **`config.py`** 加常量 `SETUP_VERSION = 1`（与 `SCHEMA_VERSION` 解耦——前者是用户引导版本、bump 节奏由「是否新增值得重配的选项」决定，后者是数据格式版本），加 `save_setup_version()` / `setup_version()` 读写 helper，字段平铺存 `~/.config/token-tracker/config.json`。② **`hooks.setup()` 末尾收口**调 `config.save_setup_version()`——所有路径（新用户首次 / wizard / `_auto_setup` / 手动 `tt setup` / 自动引导）统一汇入这里，early-return 分支（无 agent）不会到此符合语义。③ **`cli.py` 启动逻辑**加引导检测（紧挨现有 `needs_update` 块）：`is_setup() AND setup_version() < SETUP_VERSION` 时，真终端（`_should_run_wizard()`）直接弹 `run_wizard()`、非 tty / 会话内仅打印一行 `setup_outdated_hint` 提示；setup/unsetup 命令在排除列表外。④ **i18n 新增** `setup_outdated_hint`（中英），同 commit 顺手把 `codex_statusline_hint` 文案简化（独立 commit 拆开）。⑤ **测试**：`test_hooks.py` 加 4 用例（setup 写入版本、真终端→引导被调、非 tty→仅提示打印含 `tt setup`、最新→跳过），用 `SystemExit` 短路 cli.main 后续数据命令逻辑。`pytest` **120 用例**（112+8 新条目相关）通过，**仍有 2 个 `test_status.py` 历史失败**（与本次改动无关：stash 后基线已失败，属于 `_build_status_data` 过滤逻辑既有问题，未顺手修）；`ruff` 全过、`mypy` 仍 1 个历史报错（行号 451→465 偏移、非新增）。**未来用法**：每次新增值得重配的选项 → 把 `SETUP_VERSION` +1（同时记得在 wizard 里加对应步骤），老用户下次跑任意 `tt` 命令自动被引导一次。
- **2026-06-22**：**install.sh 健壮化——多安装方式阶梯 + PATH 处理**。原脚本是裸 `pip install -U`，在 Homebrew / Ubuntu 23.04+ 的 PEP 668「externally-managed」环境会直接拒装、装完 `tt` 也不一定在 PATH。改为探测阶梯：**uv `uv tool install --force`（已核实 flag）→ pipx `pipx install --force` → 自建私有 venv（`~/.local/share/token-tracker/venv` + 软链 `~/.local/bin/tt`，绕开 PEP 668、不需 sudo、不往系统装新工具）→ pip `--user`（再加 `--break-system-packages` 兜 Debian 缺 python3-venv）**。`tt setup` 用解析出的绝对路径调用（避免 PATH 未更新时 command not found）；PATH 不含 tt 目录时打印明确 `export PATH` 修复行（`:$PATH:` 包裹防子串误判，macOS 默认不含 `~/.local/bin`）。验证：`bash -n` 语法过；桩化 dry-run 强制 venv / uv 分支控制流均正确；PATH 检测三情形（在 / 不在 / 子串边界）正确。**未实跑真装**（避免 `uv tool install` 覆盖本机 dev tt）。README 中英安装段同步：curl 一键 + 手动 uv/pipx/pip 三选 + 升级（重跑幂等）/卸载说明，去掉与 venv 装法冲突的 `pip --force-reinstall`。
- **2026-06-22**：**报表柱状图基线 + `tt sessions` 取数两组改动落地并验证**（提交 `b3f4e8a` / `25fbfb7`）。`ruff check src` 全过、全量 `pytest` **112 全绿**；真实数据核对——① monthly Weekly Trend 固定最近 30 周（`12/01~6/22`）、极小周（04-20 49万 / 05-11 787万）与空白周都画更暗基线、不再显示成空格；② daily Daily Trend 固定 30 天全显示、空白/极小天暗基线托底；③ `tt sessions` 默认从「史上 cost 前 20」改成「最近 20 条」（`06-19~6/22`），codex 从 **0 条进榜 → 5 条**、按 cost 倒序展示。codex 单会话 cost 偏低（最高 $29 vs claude opus $337），故需「最近 N 条」取数才不被埋。
- **2026-06-22**：**文档同步本批改动（README 中英 + CLAUDE.md）**。修正过时点：① Codex 段——删「沿用官方 status_line + 官方字段表 + 伪 statusline 一行布局」，改为**两行布局**（L1 项目|Total 橙|Model 红、L2 Limit 5h/7d/Ctx）+ **配色跟随主题**（与 CLI 报表 / CC 状态栏三者同源）+ 不再接管官方 status_line；② 主题同源描述从「报表+状态栏」改「报表 + CC 状态栏 + Codex 伪 statusline 三者」；③ `theme.json`/`lang.json` → 统一 `config.json`（README 持久化说明 + CLAUDE.md 持久化约定）；④ wizard 步骤补「默认 Yes」、总结措辞改「键值对齐」；⑤ CLAUDE.md「statusline 烘焙」约定补 Codex `_render_codex_statusline_hook()` 注入 truecolor、`tt theme set` 重烘焙 CC+Codex 两套。
- **2026-06-22**：**wizard 署名移到末尾 sign-off 行**。欢迎行去掉尾部 `- by stormzhang`（只留 `Welcome to use token-tracker v{ver}`，仍 bold green）；总结最后空一行后追加绿色非 dim 的 sign-off `{祝你使用愉快 / Enjoy!} - by stormzhang`。新增 i18n `wizard_signoff`（zh 祝你使用愉快 / en Enjoy!），`- by stormzhang` 代码拼接两语言共用。`pytest` 110 全绿、`ruff` 过。
- **2026-06-22**：**tt daily 月份表头 + d/h 时长单位多语言，删 daily 图例署名**。① **月份表头 i18n**：热力图月份表头原硬编码 `_MONTHS = [Jan..Dec]`，改 i18n key `month_short`（zh `1月..12月` / en `Jan..Dec`）；表头渲染从「按字符索引填格」重写为 `cell_len` 宽度感知放置——中文「10月/11月/12月」占 2 列宽也按 cell 对齐、不错位（删 `_MONTHS` 常量）。② **d/h 单位 i18n**：新增 `unit_day`（天/d）、`unit_hour`（小时/h）；`format._fmt_session_duration`（sessions/status 的 Duration「活跃/跨度」列，如 `9.4h / 22d14h`）+ `heatmap` daily 概览 streak（`5/12d`）的单位跟随语言（format.py 加 `from ..i18n import t`）。**保留** `tt status` 额度段 `5h`/`7d` 不动——那是订阅限流**窗口名**（产品固定术语），非事件时长。③ **删 daily 图例署名**：`_render_legend` 去掉 `Less … More` 旁的 `tt · by stormzhang` footer + 宽度换行判断（weekly/monthly 底部署名未动）。测试：`test_heatmap` 去 `_MONTHS` 导入、改 `month_short` 语言无关断言 + 加「图例无署名」断言；`test_aggregator`/`test_status` 的 duration 硬断言改用 `t("unit_hour")` 语言无关（本机 config lang=zh，原英文硬断言会挂）。`pytest` **110 全绿**、`ruff` 全过；中英强制对照核对月份表头（`9月..6月` / `Sep..Jun`）+ 单位（`9.4小时/22天14小时` / `9.4h/22d14h`）+ 对齐。
- **2026-06-22**：**wizard 配置完成总结改键值对齐 + 文案精简**。原总结是「一行挤 3 项（语言/主题/Codex 开）+ 重启 + 下一步」整片粉色、状态用「开/关」文字、偏平不好扫。重做为**纵向键值对齐**：暗色标签 + 粉色值 + 绿 `✓` 头，状态用 `✓`(绿)/`✗`(暗)图标替代「开/关」，标签 pad 到最长键（「Codex 状态栏」）对齐值（`_disp_width` 全角感知）。文案：`Codex 伪 statusline` → user-facing `Codex 状态栏`；问题去掉「（每次回答后追加状态行）」提示；底部三行 footer——`重启 Claude Code / Codex 生效` + `更改配置可再次运行 tt setup` + `运行 tt status / daily / weekly / monthly 可直接查看报表`。i18n 删废弃 key（`wizard_summary_on/off`、`wizard_done_next`）、新增 `wizard_reconfig`/`wizard_view_reports`，中英同步。**无 Codex 时三处自动跳过**（`ask_components` 不问、`run_wizard` 步数 -1、总结不显示状态栏行），实测核对。`pytest` **110 全绿**、`ruff` 全过。
- **2026-06-22**：**Codex 伪 statusline 接入主题系统 + L1 配色橙/红 + wizard 默认 Yes**。① **接入主题**：Codex 伪 statusline 原本硬编码 mocha 真彩色常量（`GREEN`/`RED`/`PINK`/`YELLOW`/`PEACH`），切主题不变；现改为烘焙注入——`_render_codex_statusline_hook()` 注入 `themes.theme_to_statusline_ansi(config.resolve_theme())` 的 truecolor dict，脚本内 `C = __STATUSLINE_TRUECOLOR__`、所有颜色走 `C["槽位"]`。槽位映射同 CC statusline：项目 `project`、分支 `branch`、增 `added`、删 `deleted`、进度条 `bar_ok/warn/danger`、Limit·Ctx 标签 `label`。Codex TUI 实测支持 truecolor，**只注入 truecolor 一套**（不含 256 兜底、不 gate COLORTERM——避免 Codex hook 环境无 COLORTERM 时误降级）。`tt theme set` 经现成 `update_hook()`（已含 Codex 重烘焙分支）自动跟随，无需新增触发逻辑。② **L1 配色**：Total 整体取 `tokens` 槽（mocha=peach/橙）、Model 整体取 `total` 槽（mocha=red/红），标签 + 数据同色（原标签 pink、数据无色）。③ **wizard 默认 Yes**：`ask_components` 的 Codex 伪 statusline 默认从「跟随上次意图」改为**固定 `True`**（每次进向导光标都从 Yes 起、不被上次测试选的 No 带偏）。`STATUSLINE_HOOK_VERSION` 1.4→1.6（触发本机自动重烘焙）。`pytest` **110 全绿**、`ruff` 全过、`mypy` 1 历史无新增；跨主题渲染快照核对 Total/Model 颜色随 mocha/dracula/nord 变（橙 #fab387→#ffb86c→#d08770、红 #f38ba8→#ff5555→#bf616a）；本机 `update_hook()` 重烘焙到 1.6、注入式 C dict、旧硬编码常量清除，待 Codex 真机实测。
- **2026-06-22**：**Codex 虚拟 model `codex-auto-review` 改写为 gpt-5.5 + 代码审查清理两提交**。① **gpt-5.5 / 虚拟 model**（`1ceb26a`）：`adapters/codex.py` `_rewrite_virtual_model` 把 `codex-auto-review`（stop-time auto-review gate 用的虚拟 model name）→ 真实 `gpt-5.5`，不再在 Model Trend / sessions 独占一行；`cost.py` 补 gpt-5.5 内置价（input 5 / output 30，4× gpt-5、必须专属不可走系列兜底）+ `_FAMILY_FALLBACK` 加 `codex-` 前缀双保险；`tables.py` Model Trend 加 `min_pct=2` 长尾过滤、与 Project Trend 对齐；补 3 组用例（改写映射 / gpt-5.5 定价 4× / auto-review 按 gpt-5.5 兜底）。② **审查清理**（`a3ac375`）：`config.py` 删 `load_theme_config`/`load_lang_config` 两个等价于 `load_config` 的过渡别名，`cli._auto_setup`/`_theme_source` 改直接 `load_config().get("theme")`，`wizard._print_summary` 去掉冗余 intent 判断、统一以 `codex_statusline_active()` 显示开关，`test_theme.py` 同步。`pytest` **110 全绿**、`ruff` 全过、`mypy` 1 历史无新增。另**只读核查** main 分支无会话内彩色报表 hook 残留：`_setup_claude` 只写 `statusLine`、Codex 唯一 `Stop` hook 是伪 statusline、无 `commands/` 目录 / slash 模板 / `UserPromptSubmit` 等关键词；report hooks 仅存于 `feature/in-session-color-cmds` 分支。
- **2026-06-22**：**Codex 不再接管 status_line + config 文件合并 + 双因素 is_setup + CC 备份挪出**。Codex 伪 statusline 已经比官方 status_line 更全（两行 + Total + Model + 5h/7d 进度条 + Ctx）→ **再接管官方 status_line 毫无意义**，全删；`_setup_codex` 不再动 `[tui].status_line` 也不再写 `codex-backup.json`。删常量 `CODEX_STATUS_LINE`/`_SL_REGEX`/`_status_line_toml`/`_BACKUP_KEY`/`_PREV_SL_KEY`，i18n 删 `codex_already`/`codex_backup`/`codex_removed`。**config 文件统一**：`theme.json` + `lang.json` 合并 `config.json`（平铺 `schema_version` + `theme` + `lang` + `codex_faux_statusline`），`config.py` 重写——`load_config()` 严格类型校验 + fallback，缺字段/坏 JSON 都返回空 dict；老用户首次读 `config.json` 不存在自动从旧两文件迁移合并、删旧文件。**双因素 `is_setup()`**：Codex 端 = 用户意图（`config.codex_faux_statusline_intent()` 严格 bool）AND 实际装好（脚本文件 + `config.toml` 含 `codex-statusline` 特征码），任一不满足视为未配，引导重 setup——避免 stale config / 文件丢失任一单点撕裂。**CC 备份从 `settings.json` 子字段挪出**：以前藏在 `tokenTracker.previousStatusLine`、污染 agent 配置；现在迁到独立 `cc-backup.json`，老用户 setup 时 `_migrate_cc_legacy_backup` 自动挪+清掉子字段。**老用户 codex-backup.json 兼容**：`_unsetup_codex` 检测到 `codex-backup.json` 仍能恢复用户原 `status_line` + 删 backup（保留卸载还原能力，新用户从此不产生）。wizard `ask_components` 默认值用上次意图（修「每次都重问」）；`_print_summary` 显示用双因素。新 `codex_statusline_active()` 公共 API。`pytest` **110 全绿**、`ruff` 全过、`mypy` 1 历史无新增；端到端模拟老用户（旧两 config 文件 + agent 根目录旧脚本 + settings 子字段备份 + codex-backup.json）→ `hooks.setup()` 后所有迁移路径都对：config.json 合并 + 旧文件删 + CC 备份挪出 + agent 旧脚本清 + Codex 用户原 status_line 保留 + 伪 statusline hook 追加 + is_setup 返回 True。CLAUDE.md 同步。
- **2026-06-22**：**tt 产物全集中放 `~/.config/token-tracker/`，agent 根目录不再被污染**。重构动机：tt 的 statusline 脚本和缓存原本散在 `~/.claude/`/`~/.codex/` 根目录（污染 agent 自己的配置目录）；slash command 因 Claude Code 强制只扫 `~/.claude/commands/` 才必须留 agent 目录，**hook 脚本 / statusline 的 command 是绝对路径**（实测脚本放 `/tmp` 也照样跑、ccstatusline 等业界项目同理），可以集中。**新布局**：`HOOK_SCRIPT_PATH=~/.config/token-tracker/claude-statusline.py`、`CODEX_STATUSLINE_HOOK_PATH=~/.config/token-tracker/codex-statusline.py`、`STATUS_FILE=~/.config/token-tracker/tt-status.json`、`CODEX_BACKUP=~/.config/token-tracker/codex-backup.json`；hooks.py / rate_limits.py / statusline 脚本模板内嵌的 STATUS_FILE 全统一；`CLAUDE_SETTINGS`/`CODEX_CONFIG`（改 agent 配置）保留 agent 目录（必须）。**老用户迁移**：`_migrate_legacy()` 清旧位置 4 个文件（`~/.claude/tt-statusline.py`/`tt-status.json` + `~/.codex/tt-statusline.py`/`tt-backup.json`），`setup` 入口和 `_unsetup_claude` 都调；`_is_tt_cc_command()` / `_install_codex_statusline` 同时识别新（`claude-statusline`/`codex-statusline`）与旧（`tt-statusline`）特征码（旧路径残留自动迁移到新路径）。版本号 `HOOK_VERSION` 1.20→1.21、`STATUSLINE_HOOK_VERSION` 1.3→1.4（触发自动重烘焙）。`pytest` 107 全绿、`ruff` 过、`mypy` 1 历史无新增；**端到端实跑迁移**：模拟老用户（agent 根目录有 4 个旧文件 + settings.json/config.toml 指向旧路径）→ `hooks.setup()` → 旧 4 个文件全删、新位置 2 个脚本+缓存+备份全装、settings.json/config.toml 的 command 都改成新路径。CLAUDE.md 同步。
- **2026-06-22**：**会话内彩色命令（report hooks）从 main 移除，存到 `feature/in-session-color-cmds` 分支**（以后想加 `git revert` 该 commit 或 merge 分支即可）。删 `hooks.py` 的 `CC_REPORT_HOOK_SCRIPT`/`CODEX_REPORT_HOOK_SCRIPT` 模板 + `_install/_uninstall/_render/_write/_installed_*_report` 一整套 + `REPORT_HOOK_VERSION`/`CC_REPORT_HOOK_PATH`/`CC_COMMANDS_DIR`/`CODEX_REPORT_HOOK_PATH`/`_CC_REPORT_CMDS`/`_CODEX_HOOK_REGEX` + setup/unsetup/needs_update/update_hook 里的调用；`SetupComponents` 只剩 `codex_faux_statusline`（`report_hooks` 删）；`_setup_claude` 去 `components` 参数（不再用）。`wizard.py` 删「启用会话内彩色命令」那步 + 总结项，步数 `enhancement_q` 只剩 Codex 伪 statusline。`i18n` 双语删 `wizard_q_report_hooks`/`wizard_summary_report`/`cc_report_hint`/`codex_report_hint`。`tests` 删 4 个 report 测试 + 改 3 个 components 测试。**保留**：CC statusLine、Codex 伪 statusline（Stop hook）、setup/unsetup 框架。`pytest` **107 全绿**、`ruff` 过、`mypy` 1 历史无新增；隔离 HOME 实跑 setup 核对——装 CC statusLine + Codex status_line + 伪 statusline、零 report 文件。**`README` / `CLAUDE.md` 里会话内彩色命令章节待后续清理**（本次只动代码）。
- **2026-06-22**：**Codex 伪 statusline 改 CC 两行样式 + wizard 总结加语言/粉色**。① **Codex 伪 statusline 两行**（仿 CC statusline，`STATUSLINE_HOOK_VERSION` 1.0→1.3）：L1 `[项目](分支 +A -D) | Total: <会话累计> | Model: <model effort>`（去掉 Code——Codex 无现成代码行数统计、且 L1 git +A -D 已覆盖改动量）、L2 `Limit: 5h <bar> % (reset) | 7d <bar> % (reset) | <window> Ctx <bar> %`（进度条 + reset，仿 CC）。Total = 当前会话 `total_token_usage`（in+out+reasoning，transcript_path 精确定位准）；Model = session **turn_context** 的 `model`（gpt-5.5）+ `effort`（high），比 payload.model（gpt-5-codex id）友好；systemMessage 开头加 `\n` 让内容与 Codex 的 `warning:` 包裹行分开。② **wizard 配置完成总结**：加「语言」项（放第一，语言名不翻译）+ 三行从 dim 改 **mocha pink**（粉色）。③ 核实 Codex 数据：无订阅到期/Code 行数字段（只有 `plan_type: plus` 套餐类型、用量 `resets_at`）；token_count info 仅 `last/total_token_usage` + `model_context_window`。清理项目本地残留 `.codex/config.toml`（旧实验 hook 指向已删脚本、报错根源；gitignore 不进仓库）。`pytest` 111 全绿、`ruff` 过、`mypy` 1 历史无新增；本机 `~/.codex/tt-statusline.py` 重烘焙到 1.3 待 Codex 真机实测两行换行显示。
- **2026-06-21**：**`tt setup` 显示检测到的 agent + Active Days 三处统一多语言**。① **setup 显示检测结果**：`run_wizard` 欢迎行（缩进 2）下一行显示 `t('detected')`「检测到: Claude Code ✓, Codex ✓」（i18n、跟随语言）；`_run_setup_flow` 守卫保持单一入口、detected 展示移进 wizard（欢迎行后）；清掉数据命令路径那条命令全在排除列表、几乎触发不到的死 `detected`。② **Active Days 通用化**：i18n key `daily_active_days` → `active_days`（去 daily 前缀），daily 概览 / weekly This Week 卡片 / monthly This Month 卡片三处统一 `t("active_days")`（原 weekly/monthly 硬编码英文）。`test_cli_setup_wizard_or_auto` mock 改 `SimpleNamespace`（detected 需 `.name`）；`pytest` **111 全绿**、`ruff` 过、`mypy` 1 历史无新增；中文实跑核对 weekly「活跃天数: 1/7」/ monthly「活跃天数: 22/30」/ setup 欢迎+检测两行缩进对齐。mock/run.py 同步。
- **2026-06-21**：**`tt daily` 概览隐藏 Active Hour + 指标 / 星期多语言适配**。① **隐藏 Active Hour**：第三行（粉）只剩 Busiest / Top Model；连带清理整条 `hourly` 链——`_render_summary` / `render_daily_heatmap` 去 `hourly` 参数、删 `_active_hour_range` 函数、cli daily 分支（算 hourly 后并入 else `render_fn`）、孤儿 import（`defaultdict` / `UTC` / `timedelta`）。② **指标标签 i18n**：`daily_peak`（峰值）/ `daily_streak`（连续/最长）/ `daily_active_days`（活跃天数）/ `daily_busiest`（最忙），heatmap.py 硬编码英文改 `t()`。③ **星期全用「周X」全称**：`weekday_full`（busiest 值，Mon 开头）+ `weekday_grid`（热力图左侧行标签，周日开头）两个 i18n 列表；grid 标签由单字「日」改全称「周日」，加宽标签列 4→6（常量 `_INDENT`/`_LABEL_COL`/`_BLOCK_X` + `rich.cells.cell_len` pad 统一对齐），月份表头 / 图例缩进 / `_display_weeks` 周数都按 `_BLOCK_X` 自适应；英文也全称（Sun…Sat / Mon…Sun）。④ **图例 Less / More 不翻译**（硬编码英文，删 `heat_less`/`heat_more` i18n key）。`test_heatmap` 星期断言改语言无关（`t("weekday_grid")` 首尾）；`pytest` **111 全绿**、`ruff` 全过、`mypy` 1 历史无新增。中英实跑核对：中文「最忙: 周四」/ grid「周日…周六」对齐方块第 8 列、英文同构。mock/run.py 同步（本地、不进 git）。
- **2026-06-20**：**跨平台收尾——Windows 系统语言检测 + `CLAUDE_CONFIG_DIR` / `CODEX_HOME` 自定义目录**。① **系统语言加 Windows 分支**：`i18n._detect_system_lang` `sys.platform=="win32"` 走 `ctypes.windll.kernel32.GetUserDefaultUILanguage()`（LANGID 低 10 位 == `0x04` → 中文），读 Windows **用户界面语言**而非环境变量（修「Windows 中文系统但无 `LANG` 被判英文」）；ctypes 不可用优雅回退 `LANG`。+1 测试（win32 回退）。② **自定义目录环境变量**（WebSearch 查官方文档发现的 gap，跨平台非仅 Windows）：`adapters/util.py` 加 `claude_home()`（`CLAUDE_CONFIG_DIR` 逗号取首 / `~/.claude`）+ `codex_home()`（`CODEX_HOME` / `~/.codex`）两个统一 helper；`hooks.py`（全部 settings/statusline/report/缓存路径 + statusline 脚本模板内 `tt-status.json`）、`adapters/codex.py`、`claude.py`、`rate_limits.py` 全改用 helper。statusline 脚本内 `tt-status.json` 也读 `CLAUDE_CONFIG_DIR`、与 `rate_limits.STATUS_FILE` 一致，`HOOK_VERSION` 1.19→1.20。③ **WebSearch 核实**官方文档：Windows 下 `~/.claude`→`%USERPROFILE%\.claude`、`~/.codex`→`%USERPROFILE%\.codex`，`os.path.expanduser` 正确识别；`CLAUDE_CONFIG_DIR`（anthropics/claude-code#25762）/ `CODEX_HOME`（developers.openai.com/codex）官方支持覆盖。实测默认路径不变、`CLAUDE_CONFIG_DIR=/tmp/x` + `CODEX_HOME=/tmp/y` 时 settings/config 正确跟随。`pytest` **111 全绿**、`ruff` 全过、`mypy` 1 历史无新增。README 双语补跨平台/env 说明。**注**：Windows 整体仍未在真机实测（状态栏脚本解释器路径、时区回退等待验证）。
- **2026-06-20**：**配置前置校验 + Codex 检测统一 + 非交互默认全装 + 语言读系统设置**（接续交互向导）。① **Codex 检测统一为 `~/.codex` 目录**（与 CC 看 `~/.claude` 对齐）：`adapters/codex.py` `detect()` `SESSIONS_DIR` → `CODEX_DIR`、`hooks.py` 加 `CODEX_DIR` 常量、3 处 `has_codex` 由 `os.path.exists(CODEX_CONFIG)` 改 `os.path.isdir(CODEX_DIR)`、`wizard._has_codex` 同步；连带 **`_setup_codex` 在「目录在但无 `config.toml`」时新建**（`content=""` 起、写 status_line + hooks，覆盖「装了 Codex 但从没改过配置」）。② **`detect_agents()` 守卫收成单一入口** `cli._run_setup_flow()`：唯一做「装没装 agent」判断的地方（零 agent → `no_agent_install` + 返回），再按环境分流 `run_wizard()` / `_auto_setup()`；`run_wizard` 和 `_auto_setup` 内部都不再各自 detect。**数据命令改用 `is_setup()` 守卫**（不再前置 detect_agents）：没配过 → `_run_setup_flow()` 引导，引导后仍未配置 → `sys.exit(1)`；配过则 `detect_agents()` 纯拿列表渲染。修了显式 `tt setup` 原先无前置检测、零 agent 走完向导却静默失败的坑。③ **非交互环境默认全装**：`cli._auto_setup()`——非 tty/CI/会话内用默认装好（**语言跟随系统**、主题 mocha、组件全开），仅未配置过才落默认（不覆盖已选）、`auto_setup_hint` 提示。④ **语言读系统设置、绕过 CLI `LANG`**（同时区那套）：`i18n._detect_system_lang()` macOS 读 `defaults -g AppleLanguages` 首选语言、非 macOS/失败回退 `LANG`；`_detect_lang` 优先级 `config > TT_LANG > 系统检测`。实证主人机 `LANG=en_US` 但 `AppleLanguages=zh-Hans-US` → 检测 `zh`（环境变量误导、系统设置才准）。⑤ **`--mock` 本地演示钩子**（开关进 git、数据不进）：`cli.main` 检测 `--mock` → `_load_local_mock()` 加载本地 `mock/run.py`（gitignore，monkeypatch 数据源 + 概览渲染）再走正常报表流程；发布包无 `mock/` → 提示 `--mock is for local dev only` + exit 0，对普通用户零影响。`uv run tt daily --mock` 出拼字演示、不带则真实数据。⑥ **测试** +2（Codex 无 config.toml 创建 / 非 darwin 系统语言回退）+ 改 2（`tt setup` wizard/auto 分流 + `_run_setup_flow` 零 agent 不进 wizard/auto）+ 给 codex 测试加 `CODEX_DIR` 隔离；`pytest` **110 全绿**、`ruff` 全过、`mypy` 1 历史无新增。README/CLAUDE.md 同步。
- **2026-06-20**：**setup 交互式向导（questionary 上下键）+ 语言选择 + `tt setup -i`**（curl|bash 装完自动进引导）。① **底层**：`hooks.SetupComponents` dataclass（`report_hooks` / `codex_faux_statusline` 两开关；状态栏总装、不可选 —— 是 setup 的核心），`setup(components=None, quiet=False)` 不传 = 全装（向后兼容）、`quiet=True` 全静默（wizard 场景，末尾给一次综合总结）；`_setup_claude` / `_setup_codex` 按开关跳过组件 + 反向调 `_uninstall_*`（关掉时不留残留）。② **依赖**：pyproject 加 `questionary>=2.0`（拉 prompt_toolkit ~500KB，仅 wizard / `setup -i` 触发；cli 延迟 import、日常 `tt` 启动不受影响）。③ **wizard 全改 questionary**（取代 Rich `Prompt`）：流程 `欢迎行 → [1/N] 语言 → [2/N] 主题 → [3/N..] 增强项 → setup(quiet) → 综合总结`，步数随检测到的 agent 动态（CC+Codex=4 / 单 agent=3）。④ **语言选择**：`config.py` 加 `lang.json` 读写（`save_lang`/`resolve_lang`）、`i18n.py` `_detect_lang` 优先读配置 + `set_lang()` 运行时切换；wizard 第 1 步双语硬编码 prompt 选中→保存→即时切后续文案（不再只靠 `LANG` 环境变量，修「系统中文但终端 LANG=en 显英文」）。⑤ **样式**：`_wizard_style()` 固定 mocha 配色（引导界面不随所选主题变）——`?`→绿圆点 `●`、光标 `»`→`❯`(mauve)、当前行 mauve+bold、提示 dim；关掉 `(Use arrow keys)`；欢迎行固定英文 `Welcome to use token-tracker v{ver} - by stormzhang`（前段绿、署名 dim）。⑥ **主题选项内联色板 + 名字高亮**（核心难点，对 questionary 源码打补丁 `_highlight_listtitle_names`）：每行「名字 + 8 色块」同一行，光标行名字染 mauve（patch `_get_choice_tokens`：list 标题原生跳过高亮，后处理给空 style 名字 token 上色、hex 色板不动）、选完回显只留名字去色板（patch `get_pointed_at`：`is_answered` 时返回纯名字）；mocha 固定第一、「（推荐）」紧跟名字、`unicodedata.east_asian_width` 全角感知 pad 让色板列对齐。⑦ **不传 `default` 给 select**：questionary `_is_selected` 把 default 永久标 `class:selected` 致其不跟光标走（源码确认），改用 `_current_first` 把当前值重排到首位设光标起点。⑧ **`tt setup` 直接进向导**：终端 tty 下 `tt setup`（无需子参数）走完整 `run_wizard`（语言+主题+组件，每次都可重配）；非 tty（CI/Docker/管道）降级非交互 `setup()` 全装。（曾短暂用 `tt setup -i` 触发、后按主人意见去掉 flag、改无参直接进。）⑨ **`install.sh` curl 管道兼容**：`[ -t 1 ] && [ -e /dev/tty ]` → `tt < /dev/tty`（curl\|bash 下 stdin 是脚本流、必须接 tty 才能交互）；否则降级 `tt setup` 全装（CI/Docker）。⑩ **i18n** 双语重写 wizard 文案（问题一行、去尾问号、综合总结 5 条）+ 清理孤儿（`wizard_intro`/`wizard_welcome`/旧 `_title`/`_desc`/`_enable`）。⑪ **测试** +6 用例（lang roundtrip / set_lang 切换 / detect_lang 配置优先 / SetupComponents 默认+all_on / 双开关关闭不装且留用户 hook / `tt setup -i` 走 run_wizard）+ 改 `test_run_wizard_saves_theme`（mock questionary.select）。⑫ **README** 双语：用法表加 `tt setup -i`、新增「首次运行向导」节。`pytest` **107 全绿**、`ruff` 全过、`mypy` 1 历史无新增；主人本地多轮实测：上下键流畅、语言中文生效、内联色板 + 名字高亮跟光标、回显去色板、综合总结简洁。
- **2026-06-20**：**Codex 伪 statusline 产品化**（`tt setup` 自动装、`tt unsetup` 自动卸）。把可用版 `.codex/hooks/tt-statusline.py` 内嵌成 `hooks.py` 的 `CODEX_STATUSLINE_HOOK_SCRIPT` 模板（占位符 `__STATUSLINE_HOOK_VERSION__` 注入，无 subprocess 故无需 `__TT_PYTHON__`；脚本 import `token_tracker.adapters.codex` / `.adapters.util.iter_jsonl_dicts` 直接用模块），落盘 `~/.codex/tt-statusline.py`；`_setup_codex` 末尾追加 `[[hooks.Stop]]` 段（特征码 `tt-statusline`、`timeout = 10`），仿照 `_install_codex_report` 写 `_install_codex_statusline` / `_uninstall_codex_statusline` / `_CODEX_STATUSLINE_REGEX`（按 command 含 `tt-statusline` 精确定位 tt 自己的段、不动用户其它 Stop hook）。新增 `STATUSLINE_HOOK_VERSION = "1.0"` 独立于 `HOOK_VERSION`/`REPORT_HOOK_VERSION`；`needs_update()` 把 statusline 也纳入版本检测、`update_hook()` 同步重烘焙。**`is_setup()` 不纳入**——老用户升级不会被强制重 setup，新功能走 `needs_update()` 静默同步（同 CC report hook 模式）。i18n 双语加 `codex_statusline_hint`。**配色暂 mocha 硬编码**（与 CC statusline 解耦，不接主题系统：Codex hook 渲染区在 TUI `warning:` 块、与 CC statusline 不是同一渲染层、无 COLORTERM 协商）。新增 4 用例（渲染占位符替换 + 语法正确 / install·uninstall 往返保留用户其它 Stop / 版本号 roundtrip / 卸载不动用户其它 Stop hook）。清理项目根 `.codex/hooks/` 5 个实验残件（`tt-statusline.py` / `tt-render.py` / `tt_system_message.py` / `test-color.py` / `test-statusline.py`）+ `.tmp/`，保留个人本地 `config.toml`（gitignore）。`pytest` 101 全绿、`ruff` 全过、`mypy` 1 历史无新增；端到端跑 `_render_codex_statusline_hook()` 渲染产物喂假 payload 实测：项目名 + git 分支 + 增删行数 24-bit truecolor 渲染正确（无 rate_limits/Ctx 时对应段优雅隐藏）。README 双语补充 Codex 伪 statusline 段；CLAUDE.md 项目结构表加 statusline hook 路径。
- **2026-06-20**：**报表加 `--theme NAME` 命令行参数**（临时切主题对比、不落配置 / 不重烘焙状态栏）。`cli._extract_theme_arg` 在 command 解析前从 args 提取 `--theme NAME` 并移除，`theme.set_active_theme` 激活（仅本进程，优先级高于 `TT_THEME`/config）；对 daily/weekly/monthly/sessions/status 全部生效。无效主题名报错 + 列可用主题（复用 `theme_unknown`/`theme_options` 文案）。用法如 `tt monthly --theme dracula`。`test_cli.py` 加 `test_extract_theme_arg`。`pytest` 97 全绿、`ruff` 过、`mypy` 1 历史无新增；实跑核对 dracula 生效（dracula 绿 35 / mocha 0）、badname 报错、无 flag 仍默认 mocha。已提交 `bb9b260`。
- **2026-06-20**：**`tt monthly` 重构为 weekly 同款五区块卡片视图**（替换旧表格 + Model Distribution）。五块：① This Month 卡片（品牌行 + 月份 + Tokens/Cost/Avg-Cost 橙、Sessions/Msgs/Active Days 蓝，均带环比上月；Avg/Cost=本月成本÷已过天数，当前月按今天、历史月按整月，`_month_span` 用 `calendar.monthrange`；Active Days=本月活跃天/当月总天数）② Weekly Trend 按周橙色柱状图（仿 weekly 的 Daily Trend 但按周聚合，last 20 周，标签 M/DD 如 6/15，峰值标周 + 底部起止）③ Monthly Trend 逐月进度条（仿 Weekly Trend，最新在上、亮绿，本月满格其余按比例）④ Project Trend（本月，粉）⑤ Model Trend（本月，蓝）。Project Trend 过滤占比 ≤2% 的项（共用 `_render_distribution` 的 `min_pct=2`，严格保留 >2%；边界项如 2.03% 四舍五入显示 2.0% 仍保留）、Model Trend 全部显示（`min_pct=0`），weekly 报表同步生效。与 weekly 同构：weekly「Daily 柱 + Weekly 进度条」↔ monthly「Weekly 柱 + Monthly 进度条」（细粒度柱 + 粗粒度进度条，主人要按周柱）。复用 weekly 的 `brand_line`/`append_metric`/`_bar_text`/`_render_distribution`/`_merge_weeks`/`forced_color_console`；新增 `_merge_months`（跨 agent 合并同月）/`_month_span`/`_render_month_summary`/`_render_monthly_trend`/`_render_weekly_barchart`（按周柱状，周数很少时标签越界保护）。**保持合并总览**（不跟随会话，月报看全局；仅 weekly/daily 跟随）。cli：monthly 加入「去 Detected 行」列表 + 单独分支传 daily + weekly。清理本次造成的孤儿：删 `_render_monthly_table`/`_render_model_breakdown`（旧 monthly 唯一引用）+ `format._group_by_agent`（连带 `defaultdict` import）。新增 `tests/test_tables.py` 3 用例（五区块含 Weekly Trend / 多 agent 同月合并 / 空数据）。`pytest` 96 全绿、`ruff` 过、`mypy` 1 历史无新增；`tt monthly` 实跑核对五区块 + 配色 + 环比 + 按周柱状。已提交 `27df99d`。
- **2026-06-20**：**会话「活跃时长」维度 + `tt sessions` Duration 列改「活跃 / 跨度」组合显示**。① 数据层 `SessionStats` 加 `active_minutes`（`aggregate_sessions` 按相邻 entry 间隔累加，单段间隔 >30min（`ACTIVE_GAP_CAP_MIN`）视为「人离开」整段丢弃；`duration_minutes` 仍为首尾跨度）。② `tt sessions` / `tt status` 过滤短会话由 `duration_minutes>=5` 改 `active_minutes>=5`（避免挂机 / cron 长会话占位）。③ Duration 列 `_fmt_session_duration(active, span)` 组合显示——活跃恒小数小时 `9.4h`、跨度 ≥1 天 `22d14h`（整天省为 `22d`）、<1 天小数小时 `6.0h`（格式经主人定稿）；i18n `col_duration` → 「活跃/时长」/「Active/Duration」。④ statusline `Out TPS` 无数据时隐藏整项（不再显示 `-`），`HOOK_VERSION` 1.18→1.19。⑤ 清理本次工作造成的孤儿 `_fmt_duration`（HEAD 时被 status.py 调用，本次切 `_fmt_session_duration` 后无调用者、src+tests grep 0 残留）。`pytest` 93 全绿（含活跃时长大空隙丢弃 + Duration 格式 3 断言）、`ruff` 过、`mypy` 1 历史无新增；`tt sessions` 实跑核对组合格式（`9.1h / 22d17h`、`8.5h / 2d`、`6.2h / 6.2h`）。已提交 `15ed8a9`。
- **2026-06-19**：**`tt status` 改当天维度 + project 识别按 git 仓库根**。① **status 当天化**：头图 `Today` + 当前时间（系统时区今天 00:00 起，替代「过去 5h」）、底部 session 同步取当天；`_build_status_data` 改「load 过去 25h 后按系统时区今天过滤」、删 `_STATUS_HOURS`、`_render_summary` 去 5h fallback（残留 "5h" 文案 → 当天 / `[Today by Agent]`）；中间 5h/7d 额度（订阅滚动窗）不变。② **project 识别修复**：`util.project_from_cwd` 改为逐级向上找 `.git` 取仓库根目录名（`infohunter/official` → `infohunter`），解决「在子目录跑 agent 被识别成子目录名」；**纯读文件系统、不依赖 git 二进制**，三层 fallback（非仓库 / 仓库根已删 → 回退最后一段）；影响 daily/weekly/monthly/sessions/status 所有 project 列。新增 `tests/test_util.py` 4 用例。`pytest` 91 全绿、`ruff` 过、`mypy` 1（无新增）。
- **2026-06-19**：**`no_agent` / `no_data` 空态文案优化**（只动 `i18n.py`、逻辑零改）。原「未检测到任何 AI Agent」「暂无数据」过于笼统、不引导。改为：`no_agent` 点名支持范围（Claude Code / Codex）+ 判定依据（`~/.claude` / `~/.codex` 日志路径）+ 下一步；`no_data` 引导去用 agent 产生数据。两条区分清楚——没装 vs 装了没用。中英双语同步；`ruff` 过、`pytest` 87 全绿。容错本身此前已完整（一个都没装→`no_agent`+`exit(1)`；只装一个→自动只跑那个；装了无数据→`no_data`；`t()` 缺 key 返回 key 本身不崩），本次仅润色文案。
- **2026-06-19**：**`tt sessions` Model 列修正取值口径 + 支持多模型展示**。修 bug：`aggregate_sessions` 原按各 model 的 `total_tokens` 累加取 primary，而 `total_tokens` 实测 99% 是 cache_read——后台小模型（如 Haiku）读了海量上下文会被误判为会话主力（实测 9 个多模型会话里 2 个被误标 Haiku，真实生成是 Opus）。改为按 **`output_tokens`（真实生成量）选 primary**，output 持平用 total 兜底。`SessionStats` 新增 `models: dict[str,int]`（按 output 降序），Model 列**最多展示前两个**（`Opus 4.8, Haiku 4.5`），单模型照常一个。`test_aggregator.py` helper 加 `out`/`cache_read` 参数、重写 primary 测试验证「不被 cache_read 带偏」+ 加 3 模型有序断言。`pytest` 全量 87 用例全绿、`ruff` 过；`tt sessions` 实测多模型会话正确展示主力在前。
- **2026-06-19**：**`tt sessions` 重构为 status 同款两段视图 + 列样式打磨**。复用 `ui/status.py` 头图概览 + session 列表（新增 `render_sessions_view`、`_render_summary` 参数化 title/subtitle），**去掉额度段**；顶部汇总以**展示出的 session 为口径**（`cli._summary_from_sessions`）。默认 **cost 倒序** + **过滤 <5min** 后取前 20（不足全显，`--sort`/裸数字可覆盖）；标题加计数 `Recent Sessions (N)`、末尾加参数 Tips（仅 sessions、不入 status）。列序 Time·Project·Tokens·Cost·Msgs·Duration·Model·Agent；配色 Time 蓝 / Project 绿 / Tokens 橙 / Cost 黄，Tokens·Cost 各自前三（第一红、二三粉）；`AGENT_SHORT` 全局 `CC`→`Claude`。清理旧 `tables.render_sessions` 死代码 + 孤儿 import + i18n 孤儿 key（`session_summary`）。`tt status` 底部 session 列共用同步。`pytest` 全绿、`ruff` 过、`mypy` 3→2（删调用消掉一个历史报错）。
- **2026-06-19**：**Codex 伪 statusline 实测通过 + 可用版落地**（`Stop` hook + `systemMessage`）。机制实测两点均过：① 渲染 **24-bit 真彩色**（包裹 `Stop hook (completed)` + `warning:`）② **不进模型上下文**（Codex 答不出 status 数字、要查本机文件还原）。可用版 `.codex/hooks/tt-statusline.py`（Miniforge python、零依赖、`.codex/` 本地 gitignore 不进仓库）：一行 `[项目](分支+改动) | 5h/7d（+reset）| Ctx`，借 CC statusline 样式 + mocha 配色；数据 = `load_rate_limits()`（5h/7d 账号级准）+ 最近 session `last_token_usage`（Ctx）+ git。删掉 Total（最近文件近似不准）/ Cost（估算非 Codex 自带）。Ctx 已按 Stop payload 的 `transcript_path` **精确定位当前会话**（实测 payload 带 session_id/transcript_path/cwd/model，取到的 Ctx 与目标会话一致、不再串别的会话、免 rglob 全扫）；剩产品化（进 `tt setup` 自动注册）未做。详见 `docs/codex-hooks-statusline-research.md`。
- **2026-06-19**：**statusline TPS 多会话修复 + `tt status` Rate Limits 头行 + 进度条网格着色**（本会话三件，均已提交、live `update_hook()` 已重烘焙）。① **`Out TPS` 恒显 `-` 修复**：`tt-status.json` 是全会话共享单文件、会被其它会话覆盖 → `_read_prev` 按 `session_id` 对不上 → 差分取不到 → TPS 恒 `-`；改为按 `session_id` 存进 `_tps_state` dict、写入时合并保留其它会话状态、LRU 限 20，`HOOK_VERSION` 1.17→**1.18**，加 `test_statusline_tps_isolated_per_session` 回归（commit `e71496d`）。② **Rate Limits 段重构**：扁平表 → 「每 agent 一段」，头行展示该 agent 过去 5h `Tokens`/`Cost`/`Model`（去 CC `display_name` 的 `(1M context)` 后缀），下接 5h/7d 进度条；时间前缀改 `reset at`、删 `Resets` 列头；多 agent 块间空一行（commit `43886fc`）。③ **进度条未填充网格着色**：`progress_bar` 未填充 `░` 段 `pct>0` 用 `color_by_pct` 染当前档位色（░ 字形天然更淡 → 同色暗格），`pct=0` 保持灰、`value=None`（API 模式 n/a）不变，加 `test_statusline_progress_bar_empty_grid_tinted`（commit `1317cd1`）。`pytest` 全绿（+2 hooks 测试、status 断言同步）、`ruff` 全过、`mypy` 仍 3 历史无新增。
- **2026-06-19**：**清理旧交互式 dashboard 死代码**（`tt status` 已替代，旧代码无活路径调用）。删 `cli.py`（`_show_interactive_dashboard`/`_show_agent_dashboard`/`_build_agent_data`/`_DashState`/`_apply_key`/`_render_dashboard_frame`/`_read_key*`/`KEY_MAP` + `_ALT_*`/`_RECENT_BLOCK_HOURS` 常量）+ `tables.py`（`render_dashboard`/`_render_recent_sessions`）+ `panels.py`（`render_tab_bar`/`_render_daily_panel`/`_render_active_block`/`_render_idle_panel`/`_render_month_overview`）+ `widgets.py`（全部 widget，文件删除）+ `test_cli.py`（对应 dashboard 测试）。**净删 ~720 行**。保留 daily/weekly/monthly/sessions/status 共用的 `_render_header`/`_render_agent_summaries` 及 `_load_entries`/`_build_status_data`/`_current_session_agent`。逐个 grep 确认 0 残留；`pytest` 全绿、`ruff` 全过、`mypy` **5→3**（死代码里 `msvcrt.getch` 两个历史报错随之删除、无新增）、`tt status/daily/weekly/monthly/sessions` 实跑全正常。commit `6c8acb2` + `9706cfe`（删空 widgets.py）。
- **2026-06-19**：新增 **`tt status`** 命令（替代 dashboard，`tt` 无参默认进），过去 5h 实时面板三段——头图多 agent **合并**概览（Token/Cost/Sessions/Messages/Top Model，仿 daily 品牌面板）、中间 **5h/7d 额度**（CC/Codex **分开**，weekly trend 样式横条；都无订阅额度则换成 per-agent token/cost/sessions/messages 统计）、底部**近期会话**（CC+Codex **合并**、强制 **Agent 列**、按 **Cost 倒序**、cost 前三名红/橙/黄高亮、加 Duration、过滤 <5min 短会话）。新增 `cli._build_status_data` + `ui/status.py` + `types.StatusSummary`，复用 `brand_line`/`emit_metrics`/`_bar_text`/`aggregate_sessions`。**全项目时间统一系统时区**：新增 `format.system_tz()` 读 `/etc/localtime` 绕过 CLI 的 `TZ` 环境变量（主人 CLI 设 `TZ=America/Los_Angeles` 但系统是北京）；status 头图/reset/session、`tt sessions` Time 列、daily Active Hour（原硬编码 +8）、dashboard reset 全改用它；macOS/Linux 软链接解析通用、Windows 回退进程时区、`except Exception` 兜底无效时区。**TPS 限制**：`_compute_tps` 算出会显示成 0 的（output 小/Δ大）也不刷新、沿用上次值；`HOOK_VERSION` 1.16→**1.17**。`pytest` 全绿（+test_status 6 + TPS round-0 用例）、`ruff` 全过、`mypy` 5 历史无新增；多场景实跑核对（有/无额度、TZ=LA 仍北京、Agent 列、cost 前三色、<5min 过滤）。README 双语「Status 面板」节 + 使用表改 `tt status`。
- **2026-06-19**：statusline **四行布局整体重排 + 配色定稿**（按主人多轮调整）。L1 `[项目](分支 +N -N，项目名 bold)` + `Total`（会话累计 in+out+cache，新增 `_read_transcript_totals` 解析当前会话 transcript，实测 1.8MB/793 行约 6ms）+ `Cost`（CC 自带 `total_cost_usd`）+ `Code: +A -B`（标签红、+/- 绿红同 git 变动）——消耗/产出指标标签统一 **红色**（`_STATUSLINE_SLOTS` 加 `total` 槽 = red）；L2 `Limit: 5h | 7d | Ctx`；L3 `Tokens: in/out/cache`（context window 快照、**非会话累计**）+ `Out TPS`（桃色）；L4 `Model | Duration | Remote`（host 去顶级域）。删原 context 快照 `Tokens`/`Cached` 旧行、消耗指标提到 L1。**API 模式**（无 `rate_limits`）实测优雅降级：L2 只剩 Ctx、不崩溃（三边界：无字段/None/空 dict 均无报错）。`HOOK_VERSION` 1.15→**1.16**（本地连续迭代合并一次升，不每次微调都 bump——见教训）。`pytest` 全绿、`ruff` 全过、`mypy` 5 历史无新增；多次重烘焙喂真实 JSON 核对四行布局/配色/降级。README 双语状态栏表按新四行重写。
- **2026-06-19**：statusline 新增**第 4 行** `TPS | Code | Repo`，数据全来自 CC 实时 stdin、不碰 transcript、不新建持久化文件（多轮探索结论：真实 api_duration 只在 statusline stdin，历史 transcript 没有，主人否决自建持久化——重 + 隐私）。① **本轮 TPS** = `current_usage.output_tokens` ÷ Δ`total_api_duration_ms`（差分，prev 从现有 `tt-status.json` 同会话读），带单位 `tokens/s`；中间/空闲帧沿用上次值（存 `tt-status.json` 的 `_last_tps` 字段，**常驻不回落 -**）；阈值 output≥20、Δ≥500ms 过滤「刚提交未生成」帧。② **Code** = `cost.total_lines_added/removed`（本会话 Claude 写/删行数，与第 1 行 git diff 语义不同）。③ **Repo** = `workspace.repo.host`。`hooks.py` 加 `_read_prev`/`_compute_tps` + render Line 4 + main 算 TPS 存 `_last_tps`；`HOOK_VERSION` 1.13→1.15。核实结论：`output_tokens` 含 thinking token（纯思考轮 text=0 但 output 上万为铁证）、`api_duration` 含 thinking 时间 → TPS 分子分母同口径自洽。`pytest` 全绿（+2 Line 4：完成帧带单位 + 空闲帧常驻 / 无历史值显 -）、`ruff` 全过、`mypy` 5 历史无新增；实跑落盘脚本喂两帧：完成帧 `TPS: 181 tokens/s`、空闲帧沿用 181，已 `update_hook()` 重烘焙（1.15）。README 双语状态栏表补第 4 行 + 第 1 行 git diff 列。
- **2026-06-19**：statusline 加 git 增删行数——第一行分支括号内显示相对 HEAD 的未提交改动 `[proj](main* +13 -8)`（+绿 / -红，0 改动隐藏）。`hooks.py` 加 `git_diff_stat`（`git diff HEAD --numstat` 求和、跳二进制、无 commit 兜底 0）+ render 拼接；`themes.py` `_STATUSLINE_SLOTS` 加 `added`/`deleted` 两槽（复用各主题 green/red 基色、6 主题自动跟随）；`HOOK_VERSION` 1.12→1.13。`pytest` 81 全绿（+1 端到端：临时 git repo 跑落盘脚本核对 +2 -1 / 干净 repo 无 +/-）、`ruff` 全过、`mypy` 5 历史无新增；实跑落盘脚本喂真实 JSON：本项目显示 +35 -4 与 `git diff HEAD` 一致、已 `update_hook()` 重烘焙本地脚本（1.13）。
- **2026-06-19**：移除 `tt claude` / `tt codex` 两个独立子命令（交互式 dashboard 方向键已能切 agent，独立命令冗余）。`cli.py` 删 `AGENT_ALIASES` + `tt claude/codex` 处理块 + dashboard 的 `agent_filter` 分支；`i18n.py` 删 `agent_not_found` 文案、`available_cmds` 去 claude/codex（zh+en）；README 双语用法表删两行；CLAUDE.md 命令行同步。`tt claude` 现回退到「未知命令」并列可用命令、不崩溃。`pytest` 80 全绿、`ruff check src tests` 全过；实跑 `tt claude`（未知命令）、`tt`（默认 dashboard）正常。
- **2026-06-19**：statusline 颜色分层重构——默认 truecolor、不支持则降当前主题 **256 色近似**、不再适配 8 色；删 `default` 主题（THEMES 6 个真彩主题）；判断改只认 `COLORTERM=truecolor/24bit`，顺手修 macOS Terminal.app 被误发 truecolor 的坑（它无 COLORTERM、`xterm-256color` → 走 256）。`themes.py` 加 `_hex_to_256` 近似算法（mocha green #a6e3a1 → 索引 151，与旧手调值一致）+ `theme_to_statusline_ansi(depth)`；`HOOK_VERSION` 1.11→1.12。`pytest` 80 全绿、`ruff` 全过、`mypy` 5 历史无新增；实跑落盘脚本：truecolor 终端发 38;2、`xterm-256color` 无 COLORTERM 发 38;5 且 0 残留 truecolor。依据：termstandard/colors 终端清单（WebFetch 核对）。
- **2026-06-19**：按主人反馈把 statusline mocha 配色调回旧观感——`_STATUSLINE_SLOTS` 改（分支 `red` 玫红对齐旧 211、标签 `pink`、Tokens `peach`、Model/Duration `blue`），`HOOK_VERSION` 1.9→1.11 触发自动重烘焙；状态栏 token 色与 CLI 报表（sapphire 青）不再同源（主人审美选择）。注：旧 branch `38;5;211`（#ff87af）实为玫红 ≈ mocha red `#f38ba8`、非 pink，首次误映射 pink 已修正。`pytest` 79 全绿、`ruff` 全过；已 `update_hook()` 重烘焙本地脚本核对：红色 + 桃色就位、版本 1.11。
- **2026-06-18**：统一多主题系统 阶段 7（文档收尾）+ 全工程收口（7 阶段完成）。README.md / README_EN.md（功能列表 + 使用表 + 「配色主题」节）、CLAUDE.md（「主题系统约定」节 + 结构表 + 命令行）同步。最终 `uv run --extra dev pytest` **79 全绿**（test_theme.py 22）、`ruff check src tests` 全过、`mypy src` 5 历史无新增。
- **2026-06-18**：统一多主题系统 阶段 6（交互向导）。新增 `wizard.py` + cli 首次运行 `_should_run_wizard()` 判定。`pytest` 全绿（+2）、`ruff` 全过、`mypy src` 5 历史无新增；实跑：`import wizard` 无循环、`tt daily` 回归正常、判定三态单测覆盖（非 tty/会话内降级）。
- **2026-06-18**：统一多主题系统 阶段 4-5（`tt theme` 命令 + 预览）。`cli.py` 加 `theme show/list/set/preview` + 简写。`pytest` 全绿（+5）、`ruff` 全过、`mypy src` 5 历史无新增；实跑：`list` 列 7 主题 + ● 当前、`show` 显示来源、`preview dracula` 注入 5 段 dracula truecolor、`set nord` 隔离 HOME 写入 `{"theme":"nord"}` 不污染主人配置。
- **2026-06-18**：统一多主题系统 阶段 3（statusline 同源烘焙）。`hooks.py` 占位符注入主题色 + `HOOK_VERSION` 1.8→1.9。`pytest` 全绿（+1 烘焙测试）、`ruff` 全过、`mypy src` 5 历史无新增；实跑落盘脚本喂真实 JSON：mocha/dracula 各注入 truecolor 正常运行、老终端（TERM=dumb 无 COLORTERM）走 default 3-bit 兜底（0 段 truecolor）。
- **2026-06-18**：统一多主题系统 阶段 2（`_S` 运行时化、接线 CLI）。`theme.py` 改 `_SProxy` 代理 + `preview_theme`/`set_active_theme`/`heat_greens`，`heatmap.py` 随改。`pytest` 全绿（test_theme.py 16 用例）、`ruff` 全过、`mypy src` 5 历史无新增；实跑核对：mocha 默认渲染正常、`TT_THEME=dracula/latte/nord` 切换即时生效（daily 热力图色随主题变）、daily/weekly/monthly/sessions 四报表无 Traceback、`_S` 全部 16 处属性用法 ⊆ 17 槽（无漏槽）。
- **2026-06-18**：统一多主题系统 阶段 1（地基）。新增 `ui/themes.py` + `config.py` + `tests/test_theme.py`，不接线。`uv run --extra dev pytest` 全绿（test_theme.py 12 用例 + 历史无回归）、`ruff check src tests` 全过、`mypy src` 5 历史无新增。frappe/macchiato/nord/dracula 官方 hex 经 WebFetch 逐字段核对。
- **2026-06-18**：会话内彩色报表 hook 终端产品化（CC `/tt-daily`·`/tt-weekly` + Codex `ttdaily`·`ttweekly`，`tt setup` 自动配 + `unsetup` 恢复）。临时 HOME 实跑 setup/unsetup 往返：合并不覆盖用户 hook、幂等不翻倍、卸载恢复原配置、解释器烘焙 venv python；report 脚本端 daily/weekly 渲染真彩色、非命令放行。`pytest` 56 全绿（+4）、`ruff check src tests` 全过、`mypy src` 5 历史无新增。提交 `74f6b62`/`776728d`/`9d5001b`/`d4cc4de`。
- **2026-06-18**：daily/weekly 稀疏 + 窄终端自适应（Weekly Trend 统一亮绿、概览 `emit_metrics` 折行、Daily Trend 去前导空 + 中间 gap、图例署名窄终端换行对齐）+ UI 去重（`brand_line`/`append_metric` 提取）。`pytest` 52 全绿、`ruff` 全过、`mypy` 5 历史无新增；窄终端（COLUMNS=40/44）+ 宽终端（120/200）实跑核对，去重前后渲染逐字一致。提交 `42386cd`/`62fc087`/`9330c7c`/`fea9d8e`/`e654537`/`b87feb4`。
- **2026-06-18（探索，本地实验）**：会话内彩色 hook 黑科技双端跑通——CC `/tt-daily`·`/tt-weekly`（UserPromptExpansion）、Codex `ttdaily`·`ttweekly`（UserPromptSubmit），`block`+`reason` 渲染真彩色、不污染上下文（核验 `hooks.md`/`context-window.md`）；桌面版 GUI hook 支持但 ANSI 显乱码（实测），列为后续 HTML/md 适配。实验在 `.claude/`·`.codex/`（gitignore），记录存 `docs/`。
- **2026-06-18**：daily/weekly/statusline UI 收尾——Trend 进度条突出第一名、daily/weekly 署名 footer + 统一缩进 2、`Context`→`Ctx`、daily 概览删 Top Project/Longest Session、Active Hour 固定北京时区、`Max Streak`→`Current/Longest Streak`、`tt -v` 全名+作者。`uv run --extra dev pytest` 52 全绿、`ruff check src` 全过、`mypy src` 5 历史无新增；`tt daily`/`tt weekly`/`tt -v` 实跑核对，宽终端（COLUMNS=200）验证 footer/缩进对齐。提交：`0f4b2bd`/`769d514`/`a650b89`/`8cb3fdd`。
- **2026-06-18**：`tt weekly` 五区块周报 + projects 数据层 + 跟随会话 + 品牌行统一。`uv run --extra dev pytest` 52 用例全绿（+1 projects 聚合）；`ruff check src tests` 全过；`mypy src` 5 个历史基线无新增；`tt weekly` / `tt daily` 实跑校验本周/合并两种模式正常。
- **2026-06-18（调研）**：确认 **CC 与 Codex 的本地 JSONL 日志都不含成本字段**——CC 日志无 `costUSD`（扫多个文件零匹配，新版只写 token usage）、Codex `token_count` 仅有纯 token。故 CLI 报表成本一律由 `cost.py` 定价表**估算**（`calculate_cost` 里 `cost_usd` 优先分支因 `cost_usd` 恒为 None 而从不触发）；状态栏能显示 CC 自带成本是因为走 CC 实时 stdin 推送的 `total_cost_usd`（数据源不同），历史 JSONL 无法回填成本。
- **2026-06-18**：daily 概览改版 + 全 CLI 切 Catppuccin 配色。`uv run --extra dev pytest` 51 用例全绿；`ruff check src tests` 全过；`mypy src` 仍 5 个历史遗留报错、无新增；`tt daily` / `tt` 终端实跑 Mocha 配色正常，色码核对一致。
- **2026-06-16**：daily 热力图实现完成。`uv run --extra dev pytest` 51 用例全绿（原 45 + 热力图 6）；`ruff check src tests` 全过；`tt daily` 终端实跑 truecolor 热力图正常。
  `0.4.0` 仍未打 tag / 未发布；热力图作为 `0.4.0` 之后的改动，本次提交到本地 main。
